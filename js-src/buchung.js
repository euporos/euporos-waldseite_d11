// Vite entry for the booking SPA. Bundles the slick-carousel
// stylesheets so react-slick has the layout it needs without
// loading anything across origins (CSP forbids that). Output:
// public/compiled/bundle/buchung.css.
import 'slick-carousel/slick/slick.css';
import 'slick-carousel/slick/slick-theme.css';
import '../styles/buchung.scss';
